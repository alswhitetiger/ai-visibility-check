{
  "name": "ai-visibility-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute ai-visibility --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute ai-visibility --remote --file=./schema.sql",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  },
  "dependencies": {
    "linkedom": "^0.18.13",
    "robots-parser": "^3.0.1"
  }
}
